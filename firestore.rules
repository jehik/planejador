rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // recursively match all documents under users/{userId}
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
